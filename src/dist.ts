import UIDateElement from './index'

window.customElements.define('ui-date', UIDateElement)
// Reflect.defineProperty(window, UIDateElement.name, { value: UIDateElement })
export default UIDateElement
